<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:clickable="false"
    android:focusable="false"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Main Bubble Icon - CIRCULAR with glow ring -->
    <ImageView
        android:id="@+id/bubble_icon"
        android:layout_width="68dp"
        android:layout_height="68dp"
        android:background="@drawable/glow_gradient_ring"
        android:src="@drawable/ic_stremini_logo"
        android:contentDescription="Stremini AI"
        android:layout_gravity="center"
        android:elevation="12dp"
        android:scaleType="centerInside"
        android:adjustViewBounds="true"
        android:filterTouchesWhenObscured="false"
        android:antialias="true"
        android:clickable="true"
        android:focusable="true"/>

    <!-- 1: Settings / Composio Automation -->
    <ImageView
        android:id="@+id/btn_settings"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:src="@drawable/ic_settings"
        android:background="@drawable/menu_tile_bg_dark"
        android:padding="10dp"
        android:contentDescription="Settings"
        android:layout_gravity="center"
        android:elevation="8dp"
        android:scaleType="centerInside"
        android:adjustViewBounds="true"
        android:visibility="invisible"
        android:antialias="true"
        android:filterTouchesWhenObscured="false"
        android:clickable="true"
        android:focusable="true"/>

    <!-- 2: Brain / Floating Chatbot -->
    <ImageView
        android:id="@+id/btn_brain"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:src="@drawable/ic_brain"
        android:background="@drawable/menu_tile_bg_dark"
        android:padding="10dp"
        android:contentDescription="Brain"
        android:layout_gravity="center"
        android:elevation="8dp"
        android:scaleType="centerInside"
        android:adjustViewBounds="true"
        android:visibility="invisible"
        android:antialias="true"
        android:filterTouchesWhenObscured="false"
        android:clickable="true"
        android:focusable="true"/>

    <!-- 3: Keyboard / AI Keyboard Switching -->
    <ImageView
        android:id="@+id/btn_keyboard"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:src="@drawable/ic_keyboard"
        android:background="@drawable/menu_tile_bg_dark"
        android:padding="10dp"
        android:contentDescription="Keyboard"
        android:layout_gravity="center"
        android:elevation="8dp"
        android:scaleType="centerInside"
        android:adjustViewBounds="true"
        android:visibility="invisible"
        android:antialias="true"
        android:filterTouchesWhenObscured="false"
        android:clickable="true"
        android:focusable="true"/>

</FrameLayout>