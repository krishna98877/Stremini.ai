<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/panel_tab_background"
    android:layout_marginHorizontal="4dp"
    android:layout_marginVertical="4dp"
    android:padding="12dp"
    android:gravity="center_vertical"
    android:clickable="true"
    android:focusable="true">

    <!-- Text preview (for text items) -->
    <TextView
        android:id="@+id/clipboard_item_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="#EEEEEE"
        android:textSize="13sp"
        android:maxLines="2"
        android:ellipsize="end"
        android:lineSpacingMultiplier="1.2"/>

    <!-- Image thumbnail (for screenshot/image items, hidden by default) -->
    <ImageView
        android:id="@+id/clipboard_item_image"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginStart="10dp"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <!-- Clipboard icon (shown for text items) -->
    <ImageView
        android:id="@+id/clipboard_item_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="10dp"
        android:src="@drawable/ic_clipboard"
        android:scaleType="centerInside"
        android:alpha="0.5"/>

</LinearLayout>
